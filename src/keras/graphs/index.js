const FunctionalGraph = require('./functional_graph');
const SequentialGraph = require('./sequential_graph');

/**
 * @namespace graphs
 * @memberof mentality.keras
 */
module.exports = {
  FunctionalGraph,
  SequentialGraph,
};
